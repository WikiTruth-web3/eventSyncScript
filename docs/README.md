# Wiki Truth 事件同步脚本项目文档

## 主要功能

### 1. 数据获取

1. 从oasis sapphire网络中查询合约事件数据。
2. 解码事件数据（对于已验证的非代理合约，可以不需要解码），本地保存为json文件。
3. 获取ipfs网络中的json文件，并可以进行本地保存。

### 2. 数据写入supabase数据库

1. 合约事件数据写入
2. metadata数据写入
3. 写入事件区块高度

### 3. 控制

1. 通过`src\controller.ts`控制是否写入、数据类型。
